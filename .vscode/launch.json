{
    "configurations": [
        {
            "name": "Xdebug Cloud",
            "type": "php",
            "request": "launch",
            "port": 9000,
            "xdebugCloudToken": "",
            "pathMappings": {
                "/var/www/html/apitest": "${workspaceRoot}"
            }
        }
    ]
}
